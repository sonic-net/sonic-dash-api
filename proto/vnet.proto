syntax = "proto3";

package dash.vnet;

import "types.proto";

message Vnet {
    uint32 vni = 1;
    string guid = 2;
    repeated types.IpPrefix address_space = 3;
    repeated string peer_list = 4;
}
